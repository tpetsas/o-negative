# o-negative
O⁻ (O Negtive) is a cross-game, cross-DLL proxy mod injection system that works by masquerading as system DLLs like `dinput8.dll`, `xinput1_4.dll`, or `dxgi.dll`. Compatible with hundreds of Windows games.
